<div class="row mb-3">
  <div class="col-md-12">
    <h1>
      All organization emails
    </h1>

    <a href="/organization_emails/new" class="btn btn-block btn-outline-secondary">
      Add a new organization email
    </a>
  </div>
</div>

<div class="row">
  <div class="col-md-12 mb-3">
    <%= render partial: "organization_email_templates/organization_emails_search_form" %>
  </div>
</div>



<hr>

<div class="row mb-3">
  <div class="col-md-12">
    <table class="table">
      <tr>
        <th>
          ID
        </th>

        <th>
          User
        </th>

        <th>
          Status
        </th>

        <th>
          Organization email
        </th>

        <th>
          Created at
        </th>

        <th>
          Updated at
        </th>

        <th>
        </th>
      </tr>

      <% @organization_emails.each do |organization_email| %>
      <tr>
        <td>
          <%= organization_email.id %>
        </td>

        <td>
          <% if organization_email.user.present? %>
            <a href="/users/<%= organization_email.user.id %>">
              <%= organization_email.user.email %>
            </a>
          <% end %>
        </td>

        <td>
          <%= organization_email.status %>
        </td>

        <td>
          <%= organization_email.organization_email %>
        </td>

        <td>
          <%= time_ago_in_words(organization_email.created_at) %> ago
        </td>

        <td>
          <%= time_ago_in_words(organization_email.updated_at) %> ago
        </td>

        <td>
          <a href="/organization_emails/<%= organization_email.id %>">
            Show details
          </a>
        </td>
      </tr>
      <% end %>
    </table>

    <div class="row mb-3">
      <div class="col-md-12">
        <%= paginate @organization_emails, theme: "twitter-bootstrap-4" %>
      </div>
    </div>

  </div>
</div>
